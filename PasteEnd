# 1. Önce .gitignore dosyanın dolu olduğundan emin ol (daha önce konuştuğumuz içerik)

# 2. Gereksiz klasörleri GitHub'dan temizle
git rm -r --cached .idea/
git rm -r --cached src/__pycache__/
git rm -r --cached tests/__pycache__/

# 3. Bu temizliği "Lab 1" mesajıyla onayla
git add .
git commit -m "Lab 1: Repository audit, cleanup of IDE/cache files and code optimization"

# 4. GitHub'a gönder (Zorla gönderim gerekebilir çünkü rebase yaptık)
git push origin main --forceLocal backup before merge

# Conflicts:
#	.gitignore
#	src/ga_engine.py
#	tests/test_ga_engine.py

# Please enter the commit message for your changes. Lines starting
# with '#' will be ignored, and an empty message aborts the commit.
# 1. Önce .gitignore dosyanın dolu olduğundan emin ol (daha önce konuştuğumuz içerik)

# 2. Gereksiz klasörleri GitHub'dan temizle
git rm -r --cached .idea/
git rm -r --cached src/__pycache__/
git rm -r --cached tests/__pycache__/

# 3. Bu temizliği "Lab 1" mesajıyla onayla
git add .
git commit -m "Lab 1: Repository audit, cleanup of IDE/cache files and code optimization"

# 4. GitHub'a gönder (Zorla gönderim gerekebilir çünkü rebase yaptık)
Git push origin main --force#
# interactive rebase in progress; onto 203ae32
# Last command done (1 command done):
#    pick 1a63819 # Local backup before merge
# No commands remaining.
# You are currently rebasing branch 'master' on '203ae32'.
#
# Changes to be committed:
#	modified:   .gitignore
#	new file:   main.py
#	modified:   src/ga_engine.py
#	modified:   tests/test_ga_engine.py
#
